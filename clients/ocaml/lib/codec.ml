(* Generated CBOR (de)serializers for the CSIL value types. *)
(* Code generated by csilgen; DO NOT EDIT. *)

[@@@warning "-30"]

open Types

module Cbor = Csil_cbor

let rec encode_task (v : task) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "uuid", (Cbor.Text v.uuid));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "payload", (Cbor.Bytes v.payload));
         Some (Cbor.Text "timeout", (Cbor.int64 v.timeout));
         Some (Cbor.Text "priority", (Cbor.int64 v.priority));
         Some (Cbor.Text "submit_time", (Cbor.int64 v.submit_time));
         Some (Cbor.Text "update_time", (Cbor.int64 v.update_time));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
         Some (Cbor.Text "auto_target_state", (Cbor.Text v.auto_target_state));
       ])

and encode_submit_task_request (v : submit_task_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "payload", (Cbor.Bytes v.payload));
         Some (Cbor.Text "timeout", (Cbor.int64 v.timeout));
         Some (Cbor.Text "priority", (Cbor.int64 v.priority));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
         Some (Cbor.Text "auto_target_state", (Cbor.Text v.auto_target_state));
       ])

and encode_submit_task_response (v : submit_task_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_get_task_state_by_id_request (v : get_task_state_by_id_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "uuid", (Cbor.Text v.uuid));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
       ])

and encode_get_task_state_by_id_response (v : get_task_state_by_id_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_get_next_task_request (v : get_next_task_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
         Some (Cbor.Text "override_timeout", (Cbor.int64 v.override_timeout));
         Some (Cbor.Text "override_current_state", (Cbor.Text v.override_current_state));
         Some (Cbor.Text "override_auto_target_state", (Cbor.Text v.override_auto_target_state));
       ])

and encode_get_next_task_response (v : get_next_task_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_complete_task_request (v : complete_task_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "uuid", (Cbor.Text v.uuid));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
       ])

and encode_complete_task_response (v : complete_task_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_update_task_request (v : update_task_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "uuid", (Cbor.Text v.uuid));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "payload", (Cbor.Bytes v.payload));
         Some (Cbor.Text "timeout", (Cbor.int64 v.timeout));
         Some (Cbor.Text "priority", (Cbor.int64 v.priority));
         Some (Cbor.Text "new_state", (Cbor.Text v.new_state));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
         Some (Cbor.Text "auto_target_state", (Cbor.Text v.auto_target_state));
       ])

and encode_update_task_response (v : update_task_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_cancel_task_request (v : cancel_task_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "uuid", (Cbor.Text v.uuid));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "current_state", (Cbor.Text v.current_state));
       ])

and encode_cancel_task_response (v : cancel_task_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.task with Some csil_x -> Some (Cbor.Text "task", (encode_task csil_x)) | None -> None);
       ])

and encode_clean_up_timed_out_request (v : clean_up_timed_out_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "at_time", (Cbor.int64 v.at_time));
       ])

and encode_clean_up_timed_out_response (v : clean_up_timed_out_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "timed_out", (Cbor.int64 v.timed_out));
       ])

and encode_get_queues_request (_v : get_queues_request) : Cbor.t = Cbor.Map []

and encode_get_queues_response (v : get_queues_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queues", (Cbor.Array (List.map (fun csil_e -> (Cbor.Text csil_e)) v.queues)));
         Some (Cbor.Text "total_task_count", (Cbor.int64 v.total_task_count));
       ])

and encode_get_queue_task_counts_request (_v : get_queue_task_counts_request) : Cbor.t = Cbor.Map []

and encode_get_queue_task_counts_response (v : get_queue_task_counts_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue_counts", (Cbor.Map (List.map (fun (csil_k, csil_v) -> ((Cbor.Text csil_k), (Cbor.int64 csil_v))) v.queue_counts)));
         Some (Cbor.Text "total_task_count", (Cbor.int64 v.total_task_count));
       ])

and encode_get_task_state_counts_request (v : get_task_state_counts_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
       ])

and encode_get_task_state_counts_response (v : get_task_state_counts_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "count", (Cbor.int64 v.count));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "state_counts", (Cbor.Map (List.map (fun (csil_k, csil_v) -> ((Cbor.Text csil_k), (Cbor.int64 csil_v))) v.state_counts)));
       ])

and encode_queue_and_state_counts (v : queue_and_state_counts) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "count", (Cbor.int64 v.count));
         Some (Cbor.Text "queue", (Cbor.Text v.queue));
         Some (Cbor.Text "state_counts", (Cbor.Map (List.map (fun (csil_k, csil_v) -> ((Cbor.Text csil_k), (Cbor.int64 csil_v))) v.state_counts)));
       ])

and encode_get_queue_and_state_counts_request (_v : get_queue_and_state_counts_request) : Cbor.t = Cbor.Map []

and encode_get_queue_and_state_counts_response (v : get_queue_and_state_counts_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "queue_and_state_counts", (Cbor.Map (List.map (fun (csil_k, csil_v) -> ((Cbor.Text csil_k), (encode_queue_and_state_counts csil_v))) v.queue_and_state_counts)));
       ])

and encode_service_error (v : service_error) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "code", (Cbor.int64 v.code));
         Some (Cbor.Text "message", (Cbor.Text v.message));
       ])

let rec decode_task (csil_c : Cbor.t) : task =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        uuid = (Cbor.to_text (csil_req "uuid"));
        queue = (Cbor.to_text (csil_req "queue"));
        payload = (Cbor.to_bytes (csil_req "payload"));
        timeout = (Cbor.to_i64 (csil_req "timeout"));
        priority = (Cbor.to_i64 (csil_req "priority"));
        submit_time = (Cbor.to_i64 (csil_req "submit_time"));
        update_time = (Cbor.to_i64 (csil_req "update_time"));
        current_state = (Cbor.to_text (csil_req "current_state"));
        auto_target_state = (Cbor.to_text (csil_req "auto_target_state"));
      }
  | _ -> failwith "csilgen: expected map for task"

and decode_submit_task_request (csil_c : Cbor.t) : submit_task_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue = (Cbor.to_text (csil_req "queue"));
        payload = (Cbor.to_bytes (csil_req "payload"));
        timeout = (Cbor.to_i64 (csil_req "timeout"));
        priority = (Cbor.to_i64 (csil_req "priority"));
        current_state = (Cbor.to_text (csil_req "current_state"));
        auto_target_state = (Cbor.to_text (csil_req "auto_target_state"));
      }
  | _ -> failwith "csilgen: expected map for submit_task_request"

and decode_submit_task_response (csil_c : Cbor.t) : submit_task_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for submit_task_response"

and decode_get_task_state_by_id_request (csil_c : Cbor.t) : get_task_state_by_id_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        uuid = (Cbor.to_text (csil_req "uuid"));
        queue = (Cbor.to_text (csil_req "queue"));
      }
  | _ -> failwith "csilgen: expected map for get_task_state_by_id_request"

and decode_get_task_state_by_id_response (csil_c : Cbor.t) : get_task_state_by_id_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for get_task_state_by_id_response"

and decode_get_next_task_request (csil_c : Cbor.t) : get_next_task_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue = (Cbor.to_text (csil_req "queue"));
        current_state = (Cbor.to_text (csil_req "current_state"));
        override_timeout = (Cbor.to_i64 (csil_req "override_timeout"));
        override_current_state = (Cbor.to_text (csil_req "override_current_state"));
        override_auto_target_state = (Cbor.to_text (csil_req "override_auto_target_state"));
      }
  | _ -> failwith "csilgen: expected map for get_next_task_request"

and decode_get_next_task_response (csil_c : Cbor.t) : get_next_task_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for get_next_task_response"

and decode_complete_task_request (csil_c : Cbor.t) : complete_task_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        uuid = (Cbor.to_text (csil_req "uuid"));
        queue = (Cbor.to_text (csil_req "queue"));
        current_state = (Cbor.to_text (csil_req "current_state"));
      }
  | _ -> failwith "csilgen: expected map for complete_task_request"

and decode_complete_task_response (csil_c : Cbor.t) : complete_task_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for complete_task_response"

and decode_update_task_request (csil_c : Cbor.t) : update_task_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        uuid = (Cbor.to_text (csil_req "uuid"));
        queue = (Cbor.to_text (csil_req "queue"));
        payload = (Cbor.to_bytes (csil_req "payload"));
        timeout = (Cbor.to_i64 (csil_req "timeout"));
        priority = (Cbor.to_i64 (csil_req "priority"));
        new_state = (Cbor.to_text (csil_req "new_state"));
        current_state = (Cbor.to_text (csil_req "current_state"));
        auto_target_state = (Cbor.to_text (csil_req "auto_target_state"));
      }
  | _ -> failwith "csilgen: expected map for update_task_request"

and decode_update_task_response (csil_c : Cbor.t) : update_task_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for update_task_response"

and decode_cancel_task_request (csil_c : Cbor.t) : cancel_task_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        uuid = (Cbor.to_text (csil_req "uuid"));
        queue = (Cbor.to_text (csil_req "queue"));
        current_state = (Cbor.to_text (csil_req "current_state"));
      }
  | _ -> failwith "csilgen: expected map for cancel_task_request"

and decode_cancel_task_response (csil_c : Cbor.t) : cancel_task_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task = (match csil_field "task" with Some csil_v -> Some (decode_task csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for cancel_task_response"

and decode_clean_up_timed_out_request (csil_c : Cbor.t) : clean_up_timed_out_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue = (Cbor.to_text (csil_req "queue"));
        at_time = (Cbor.to_i64 (csil_req "at_time"));
      }
  | _ -> failwith "csilgen: expected map for clean_up_timed_out_request"

and decode_clean_up_timed_out_response (csil_c : Cbor.t) : clean_up_timed_out_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        timed_out = (Cbor.to_i64 (csil_req "timed_out"));
      }
  | _ -> failwith "csilgen: expected map for clean_up_timed_out_response"

and decode_get_queues_request (csil_c : Cbor.t) : get_queues_request =
  match csil_c with Cbor.Map _ -> () | _ -> failwith "csilgen: expected map for get_queues_request"

and decode_get_queues_response (csil_c : Cbor.t) : get_queues_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queues = (match (csil_req "queues") with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_text csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        total_task_count = (Cbor.to_i64 (csil_req "total_task_count"));
      }
  | _ -> failwith "csilgen: expected map for get_queues_response"

and decode_get_queue_task_counts_request (csil_c : Cbor.t) : get_queue_task_counts_request =
  match csil_c with Cbor.Map _ -> () | _ -> failwith "csilgen: expected map for get_queue_task_counts_request"

and decode_get_queue_task_counts_response (csil_c : Cbor.t) : get_queue_task_counts_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue_counts = (match (csil_req "queue_counts") with Cbor.Map csil_kvs -> List.map (fun (csil_k, csil_v) -> ((Cbor.to_text csil_k), (Cbor.to_i64 csil_v))) csil_kvs | _ -> failwith "csilgen: expected map");
        total_task_count = (Cbor.to_i64 (csil_req "total_task_count"));
      }
  | _ -> failwith "csilgen: expected map for get_queue_task_counts_response"

and decode_get_task_state_counts_request (csil_c : Cbor.t) : get_task_state_counts_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue = (Cbor.to_text (csil_req "queue"));
      }
  | _ -> failwith "csilgen: expected map for get_task_state_counts_request"

and decode_get_task_state_counts_response (csil_c : Cbor.t) : get_task_state_counts_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        count = (Cbor.to_i64 (csil_req "count"));
        queue = (Cbor.to_text (csil_req "queue"));
        state_counts = (match (csil_req "state_counts") with Cbor.Map csil_kvs -> List.map (fun (csil_k, csil_v) -> ((Cbor.to_text csil_k), (Cbor.to_i64 csil_v))) csil_kvs | _ -> failwith "csilgen: expected map");
      }
  | _ -> failwith "csilgen: expected map for get_task_state_counts_response"

and decode_queue_and_state_counts (csil_c : Cbor.t) : queue_and_state_counts =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        count = (Cbor.to_i64 (csil_req "count"));
        queue = (Cbor.to_text (csil_req "queue"));
        state_counts = (match (csil_req "state_counts") with Cbor.Map csil_kvs -> List.map (fun (csil_k, csil_v) -> ((Cbor.to_text csil_k), (Cbor.to_i64 csil_v))) csil_kvs | _ -> failwith "csilgen: expected map");
      }
  | _ -> failwith "csilgen: expected map for queue_and_state_counts"

and decode_get_queue_and_state_counts_request (csil_c : Cbor.t) : get_queue_and_state_counts_request =
  match csil_c with Cbor.Map _ -> () | _ -> failwith "csilgen: expected map for get_queue_and_state_counts_request"

and decode_get_queue_and_state_counts_response (csil_c : Cbor.t) : get_queue_and_state_counts_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        queue_and_state_counts = (match (csil_req "queue_and_state_counts") with Cbor.Map csil_kvs -> List.map (fun (csil_k, csil_v) -> ((Cbor.to_text csil_k), (decode_queue_and_state_counts csil_v))) csil_kvs | _ -> failwith "csilgen: expected map");
      }
  | _ -> failwith "csilgen: expected map for get_queue_and_state_counts_response"

and decode_service_error (csil_c : Cbor.t) : service_error =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        code = (Cbor.to_i64 (csil_req "code"));
        message = (Cbor.to_text (csil_req "message"));
      }
  | _ -> failwith "csilgen: expected map for service_error"

let encode_task_bytes (v : task) : bytes = Cbor.encode (encode_task v)
let decode_task_bytes (b : bytes) : task =
  match Cbor.decode b with Ok c -> decode_task c | Error e -> failwith e

let encode_submit_task_request_bytes (v : submit_task_request) : bytes = Cbor.encode (encode_submit_task_request v)
let decode_submit_task_request_bytes (b : bytes) : submit_task_request =
  match Cbor.decode b with Ok c -> decode_submit_task_request c | Error e -> failwith e

let encode_submit_task_response_bytes (v : submit_task_response) : bytes = Cbor.encode (encode_submit_task_response v)
let decode_submit_task_response_bytes (b : bytes) : submit_task_response =
  match Cbor.decode b with Ok c -> decode_submit_task_response c | Error e -> failwith e

let encode_get_task_state_by_id_request_bytes (v : get_task_state_by_id_request) : bytes = Cbor.encode (encode_get_task_state_by_id_request v)
let decode_get_task_state_by_id_request_bytes (b : bytes) : get_task_state_by_id_request =
  match Cbor.decode b with Ok c -> decode_get_task_state_by_id_request c | Error e -> failwith e

let encode_get_task_state_by_id_response_bytes (v : get_task_state_by_id_response) : bytes = Cbor.encode (encode_get_task_state_by_id_response v)
let decode_get_task_state_by_id_response_bytes (b : bytes) : get_task_state_by_id_response =
  match Cbor.decode b with Ok c -> decode_get_task_state_by_id_response c | Error e -> failwith e

let encode_get_next_task_request_bytes (v : get_next_task_request) : bytes = Cbor.encode (encode_get_next_task_request v)
let decode_get_next_task_request_bytes (b : bytes) : get_next_task_request =
  match Cbor.decode b with Ok c -> decode_get_next_task_request c | Error e -> failwith e

let encode_get_next_task_response_bytes (v : get_next_task_response) : bytes = Cbor.encode (encode_get_next_task_response v)
let decode_get_next_task_response_bytes (b : bytes) : get_next_task_response =
  match Cbor.decode b with Ok c -> decode_get_next_task_response c | Error e -> failwith e

let encode_complete_task_request_bytes (v : complete_task_request) : bytes = Cbor.encode (encode_complete_task_request v)
let decode_complete_task_request_bytes (b : bytes) : complete_task_request =
  match Cbor.decode b with Ok c -> decode_complete_task_request c | Error e -> failwith e

let encode_complete_task_response_bytes (v : complete_task_response) : bytes = Cbor.encode (encode_complete_task_response v)
let decode_complete_task_response_bytes (b : bytes) : complete_task_response =
  match Cbor.decode b with Ok c -> decode_complete_task_response c | Error e -> failwith e

let encode_update_task_request_bytes (v : update_task_request) : bytes = Cbor.encode (encode_update_task_request v)
let decode_update_task_request_bytes (b : bytes) : update_task_request =
  match Cbor.decode b with Ok c -> decode_update_task_request c | Error e -> failwith e

let encode_update_task_response_bytes (v : update_task_response) : bytes = Cbor.encode (encode_update_task_response v)
let decode_update_task_response_bytes (b : bytes) : update_task_response =
  match Cbor.decode b with Ok c -> decode_update_task_response c | Error e -> failwith e

let encode_cancel_task_request_bytes (v : cancel_task_request) : bytes = Cbor.encode (encode_cancel_task_request v)
let decode_cancel_task_request_bytes (b : bytes) : cancel_task_request =
  match Cbor.decode b with Ok c -> decode_cancel_task_request c | Error e -> failwith e

let encode_cancel_task_response_bytes (v : cancel_task_response) : bytes = Cbor.encode (encode_cancel_task_response v)
let decode_cancel_task_response_bytes (b : bytes) : cancel_task_response =
  match Cbor.decode b with Ok c -> decode_cancel_task_response c | Error e -> failwith e

let encode_clean_up_timed_out_request_bytes (v : clean_up_timed_out_request) : bytes = Cbor.encode (encode_clean_up_timed_out_request v)
let decode_clean_up_timed_out_request_bytes (b : bytes) : clean_up_timed_out_request =
  match Cbor.decode b with Ok c -> decode_clean_up_timed_out_request c | Error e -> failwith e

let encode_clean_up_timed_out_response_bytes (v : clean_up_timed_out_response) : bytes = Cbor.encode (encode_clean_up_timed_out_response v)
let decode_clean_up_timed_out_response_bytes (b : bytes) : clean_up_timed_out_response =
  match Cbor.decode b with Ok c -> decode_clean_up_timed_out_response c | Error e -> failwith e

let encode_get_queues_request_bytes (v : get_queues_request) : bytes = Cbor.encode (encode_get_queues_request v)
let decode_get_queues_request_bytes (b : bytes) : get_queues_request =
  match Cbor.decode b with Ok c -> decode_get_queues_request c | Error e -> failwith e

let encode_get_queues_response_bytes (v : get_queues_response) : bytes = Cbor.encode (encode_get_queues_response v)
let decode_get_queues_response_bytes (b : bytes) : get_queues_response =
  match Cbor.decode b with Ok c -> decode_get_queues_response c | Error e -> failwith e

let encode_get_queue_task_counts_request_bytes (v : get_queue_task_counts_request) : bytes = Cbor.encode (encode_get_queue_task_counts_request v)
let decode_get_queue_task_counts_request_bytes (b : bytes) : get_queue_task_counts_request =
  match Cbor.decode b with Ok c -> decode_get_queue_task_counts_request c | Error e -> failwith e

let encode_get_queue_task_counts_response_bytes (v : get_queue_task_counts_response) : bytes = Cbor.encode (encode_get_queue_task_counts_response v)
let decode_get_queue_task_counts_response_bytes (b : bytes) : get_queue_task_counts_response =
  match Cbor.decode b with Ok c -> decode_get_queue_task_counts_response c | Error e -> failwith e

let encode_get_task_state_counts_request_bytes (v : get_task_state_counts_request) : bytes = Cbor.encode (encode_get_task_state_counts_request v)
let decode_get_task_state_counts_request_bytes (b : bytes) : get_task_state_counts_request =
  match Cbor.decode b with Ok c -> decode_get_task_state_counts_request c | Error e -> failwith e

let encode_get_task_state_counts_response_bytes (v : get_task_state_counts_response) : bytes = Cbor.encode (encode_get_task_state_counts_response v)
let decode_get_task_state_counts_response_bytes (b : bytes) : get_task_state_counts_response =
  match Cbor.decode b with Ok c -> decode_get_task_state_counts_response c | Error e -> failwith e

let encode_queue_and_state_counts_bytes (v : queue_and_state_counts) : bytes = Cbor.encode (encode_queue_and_state_counts v)
let decode_queue_and_state_counts_bytes (b : bytes) : queue_and_state_counts =
  match Cbor.decode b with Ok c -> decode_queue_and_state_counts c | Error e -> failwith e

let encode_get_queue_and_state_counts_request_bytes (v : get_queue_and_state_counts_request) : bytes = Cbor.encode (encode_get_queue_and_state_counts_request v)
let decode_get_queue_and_state_counts_request_bytes (b : bytes) : get_queue_and_state_counts_request =
  match Cbor.decode b with Ok c -> decode_get_queue_and_state_counts_request c | Error e -> failwith e

let encode_get_queue_and_state_counts_response_bytes (v : get_queue_and_state_counts_response) : bytes = Cbor.encode (encode_get_queue_and_state_counts_response v)
let decode_get_queue_and_state_counts_response_bytes (b : bytes) : get_queue_and_state_counts_response =
  match Cbor.decode b with Ok c -> decode_get_queue_and_state_counts_response c | Error e -> failwith e

let encode_service_error_bytes (v : service_error) : bytes = Cbor.encode (encode_service_error v)
let decode_service_error_bytes (b : bytes) : service_error =
  match Cbor.decode b with Ok c -> decode_service_error c | Error e -> failwith e
