"use strict";
// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-codec
Object.defineProperty(exports, "__esModule", { value: true });
exports.encodeValue = encodeValue;
exports.decode = decode;
exports.mapGet = mapGet;
exports.requireKey = requireKey;
exports.asNumber = asNumber;
exports.asString = asString;
exports.asBytes = asBytes;
exports.asBool = asBool;
exports.asArray = asArray;
exports.asMap = asMap;
exports.asTimestamp = asTimestamp;
exports.asDecimalPayload = asDecimalPayload;
exports.csilTsToText = csilTsToText;
exports.toTaskCborValue = toTaskCborValue;
exports.fromTaskCborValue = fromTaskCborValue;
exports.toTaskCbor = toTaskCbor;
exports.fromTaskCbor = fromTaskCbor;
exports.toSubmitTaskRequestCborValue = toSubmitTaskRequestCborValue;
exports.fromSubmitTaskRequestCborValue = fromSubmitTaskRequestCborValue;
exports.toSubmitTaskRequestCbor = toSubmitTaskRequestCbor;
exports.fromSubmitTaskRequestCbor = fromSubmitTaskRequestCbor;
exports.toSubmitTaskResponseCborValue = toSubmitTaskResponseCborValue;
exports.fromSubmitTaskResponseCborValue = fromSubmitTaskResponseCborValue;
exports.toSubmitTaskResponseCbor = toSubmitTaskResponseCbor;
exports.fromSubmitTaskResponseCbor = fromSubmitTaskResponseCbor;
exports.toGetTaskStateByIDRequestCborValue = toGetTaskStateByIDRequestCborValue;
exports.fromGetTaskStateByIDRequestCborValue = fromGetTaskStateByIDRequestCborValue;
exports.toGetTaskStateByIDRequestCbor = toGetTaskStateByIDRequestCbor;
exports.fromGetTaskStateByIDRequestCbor = fromGetTaskStateByIDRequestCbor;
exports.toGetTaskStateByIDResponseCborValue = toGetTaskStateByIDResponseCborValue;
exports.fromGetTaskStateByIDResponseCborValue = fromGetTaskStateByIDResponseCborValue;
exports.toGetTaskStateByIDResponseCbor = toGetTaskStateByIDResponseCbor;
exports.fromGetTaskStateByIDResponseCbor = fromGetTaskStateByIDResponseCbor;
exports.toGetNextTaskRequestCborValue = toGetNextTaskRequestCborValue;
exports.fromGetNextTaskRequestCborValue = fromGetNextTaskRequestCborValue;
exports.toGetNextTaskRequestCbor = toGetNextTaskRequestCbor;
exports.fromGetNextTaskRequestCbor = fromGetNextTaskRequestCbor;
exports.toGetNextTaskResponseCborValue = toGetNextTaskResponseCborValue;
exports.fromGetNextTaskResponseCborValue = fromGetNextTaskResponseCborValue;
exports.toGetNextTaskResponseCbor = toGetNextTaskResponseCbor;
exports.fromGetNextTaskResponseCbor = fromGetNextTaskResponseCbor;
exports.toCompleteTaskRequestCborValue = toCompleteTaskRequestCborValue;
exports.fromCompleteTaskRequestCborValue = fromCompleteTaskRequestCborValue;
exports.toCompleteTaskRequestCbor = toCompleteTaskRequestCbor;
exports.fromCompleteTaskRequestCbor = fromCompleteTaskRequestCbor;
exports.toCompleteTaskResponseCborValue = toCompleteTaskResponseCborValue;
exports.fromCompleteTaskResponseCborValue = fromCompleteTaskResponseCborValue;
exports.toCompleteTaskResponseCbor = toCompleteTaskResponseCbor;
exports.fromCompleteTaskResponseCbor = fromCompleteTaskResponseCbor;
exports.toUpdateTaskRequestCborValue = toUpdateTaskRequestCborValue;
exports.fromUpdateTaskRequestCborValue = fromUpdateTaskRequestCborValue;
exports.toUpdateTaskRequestCbor = toUpdateTaskRequestCbor;
exports.fromUpdateTaskRequestCbor = fromUpdateTaskRequestCbor;
exports.toUpdateTaskResponseCborValue = toUpdateTaskResponseCborValue;
exports.fromUpdateTaskResponseCborValue = fromUpdateTaskResponseCborValue;
exports.toUpdateTaskResponseCbor = toUpdateTaskResponseCbor;
exports.fromUpdateTaskResponseCbor = fromUpdateTaskResponseCbor;
exports.toCancelTaskRequestCborValue = toCancelTaskRequestCborValue;
exports.fromCancelTaskRequestCborValue = fromCancelTaskRequestCborValue;
exports.toCancelTaskRequestCbor = toCancelTaskRequestCbor;
exports.fromCancelTaskRequestCbor = fromCancelTaskRequestCbor;
exports.toCancelTaskResponseCborValue = toCancelTaskResponseCborValue;
exports.fromCancelTaskResponseCborValue = fromCancelTaskResponseCborValue;
exports.toCancelTaskResponseCbor = toCancelTaskResponseCbor;
exports.fromCancelTaskResponseCbor = fromCancelTaskResponseCbor;
exports.toCleanUpTimedOutRequestCborValue = toCleanUpTimedOutRequestCborValue;
exports.fromCleanUpTimedOutRequestCborValue = fromCleanUpTimedOutRequestCborValue;
exports.toCleanUpTimedOutRequestCbor = toCleanUpTimedOutRequestCbor;
exports.fromCleanUpTimedOutRequestCbor = fromCleanUpTimedOutRequestCbor;
exports.toCleanUpTimedOutResponseCborValue = toCleanUpTimedOutResponseCborValue;
exports.fromCleanUpTimedOutResponseCborValue = fromCleanUpTimedOutResponseCborValue;
exports.toCleanUpTimedOutResponseCbor = toCleanUpTimedOutResponseCbor;
exports.fromCleanUpTimedOutResponseCbor = fromCleanUpTimedOutResponseCbor;
exports.toGetQueuesRequestCborValue = toGetQueuesRequestCborValue;
exports.fromGetQueuesRequestCborValue = fromGetQueuesRequestCborValue;
exports.toGetQueuesRequestCbor = toGetQueuesRequestCbor;
exports.fromGetQueuesRequestCbor = fromGetQueuesRequestCbor;
exports.toGetQueuesResponseCborValue = toGetQueuesResponseCborValue;
exports.fromGetQueuesResponseCborValue = fromGetQueuesResponseCborValue;
exports.toGetQueuesResponseCbor = toGetQueuesResponseCbor;
exports.fromGetQueuesResponseCbor = fromGetQueuesResponseCbor;
exports.toGetQueueTaskCountsRequestCborValue = toGetQueueTaskCountsRequestCborValue;
exports.fromGetQueueTaskCountsRequestCborValue = fromGetQueueTaskCountsRequestCborValue;
exports.toGetQueueTaskCountsRequestCbor = toGetQueueTaskCountsRequestCbor;
exports.fromGetQueueTaskCountsRequestCbor = fromGetQueueTaskCountsRequestCbor;
exports.toGetQueueTaskCountsResponseCborValue = toGetQueueTaskCountsResponseCborValue;
exports.fromGetQueueTaskCountsResponseCborValue = fromGetQueueTaskCountsResponseCborValue;
exports.toGetQueueTaskCountsResponseCbor = toGetQueueTaskCountsResponseCbor;
exports.fromGetQueueTaskCountsResponseCbor = fromGetQueueTaskCountsResponseCbor;
exports.toGetTaskStateCountsRequestCborValue = toGetTaskStateCountsRequestCborValue;
exports.fromGetTaskStateCountsRequestCborValue = fromGetTaskStateCountsRequestCborValue;
exports.toGetTaskStateCountsRequestCbor = toGetTaskStateCountsRequestCbor;
exports.fromGetTaskStateCountsRequestCbor = fromGetTaskStateCountsRequestCbor;
exports.toGetTaskStateCountsResponseCborValue = toGetTaskStateCountsResponseCborValue;
exports.fromGetTaskStateCountsResponseCborValue = fromGetTaskStateCountsResponseCborValue;
exports.toGetTaskStateCountsResponseCbor = toGetTaskStateCountsResponseCbor;
exports.fromGetTaskStateCountsResponseCbor = fromGetTaskStateCountsResponseCbor;
exports.toQueueAndStateCountsCborValue = toQueueAndStateCountsCborValue;
exports.fromQueueAndStateCountsCborValue = fromQueueAndStateCountsCborValue;
exports.toQueueAndStateCountsCbor = toQueueAndStateCountsCbor;
exports.fromQueueAndStateCountsCbor = fromQueueAndStateCountsCbor;
exports.toGetQueueAndStateCountsRequestCborValue = toGetQueueAndStateCountsRequestCborValue;
exports.fromGetQueueAndStateCountsRequestCborValue = fromGetQueueAndStateCountsRequestCborValue;
exports.toGetQueueAndStateCountsRequestCbor = toGetQueueAndStateCountsRequestCbor;
exports.fromGetQueueAndStateCountsRequestCbor = fromGetQueueAndStateCountsRequestCbor;
exports.toGetQueueAndStateCountsResponseCborValue = toGetQueueAndStateCountsResponseCborValue;
exports.fromGetQueueAndStateCountsResponseCborValue = fromGetQueueAndStateCountsResponseCborValue;
exports.toGetQueueAndStateCountsResponseCbor = toGetQueueAndStateCountsResponseCbor;
exports.fromGetQueueAndStateCountsResponseCbor = fromGetQueueAndStateCountsResponseCbor;
exports.toServiceErrorCborValue = toServiceErrorCborValue;
exports.fromServiceErrorCborValue = fromServiceErrorCborValue;
exports.toServiceErrorCbor = toServiceErrorCbor;
exports.fromServiceErrorCbor = fromServiceErrorCbor;
const csilTextEncoder = new TextEncoder();
const csilTextDecoder = new TextDecoder();
function head(major, n, out) {
    const mt = major << 5;
    const big = typeof n === "bigint" ? n : BigInt(n);
    if (big < 24n) {
        out.push(mt | Number(big));
    }
    else if (big < 0x100n) {
        out.push(mt | 24, Number(big) & 0xff);
    }
    else if (big < 0x10000n) {
        out.push(mt | 25, Number((big >> 8n) & 0xffn), Number(big & 0xffn));
    }
    else if (big < 0x100000000n) {
        out.push(mt | 26);
        for (let s = 24n; s >= 0n; s -= 8n)
            out.push(Number((big >> s) & 0xffn));
    }
    else {
        out.push(mt | 27);
        for (let s = 56n; s >= 0n; s -= 8n)
            out.push(Number((big >> s) & 0xffn));
    }
}
function encInt(n, out) {
    if (n >= 0n)
        head(0, n, out);
    else
        head(1, -1n - n, out);
}
function encFloat(d, out) {
    out.push(0xfb);
    const view = new DataView(new ArrayBuffer(8));
    view.setFloat64(0, d, false);
    for (let i = 0; i < 8; i++)
        out.push(view.getUint8(i));
}
function encInto(v, out) {
    if (typeof v === "number") {
        if (Number.isInteger(v))
            encInt(BigInt(v), out);
        else
            encFloat(v, out);
    }
    else if (typeof v === "bigint") {
        encInt(v, out);
    }
    else if (typeof v === "boolean") {
        out.push(v ? 0xf5 : 0xf4);
    }
    else if (v === null) {
        out.push(0xf6);
    }
    else if (typeof v === "string") {
        const bytes = csilTextEncoder.encode(v);
        head(3, bytes.length, out);
        for (const b of bytes)
            out.push(b);
    }
    else if (v instanceof Uint8Array) {
        head(2, v.length, out);
        for (const b of v)
            out.push(b);
    }
    else if (Array.isArray(v)) {
        head(4, v.length, out);
        for (const x of v)
            encInto(x, out);
    }
    else if (v instanceof Map) {
        head(5, v.size, out);
        for (const [k, val] of v) {
            encInto(k, out);
            encInto(val, out);
        }
    }
    else {
        head(6, v.tag, out);
        encInto(v.value, out);
    }
}
/** Encode a CBOR value tree to canonical CSIL CBOR bytes. */
function encodeValue(value) {
    const out = [];
    encInto(value, out);
    return Uint8Array.from(out);
}
function readArg(st, low) {
    if (low < 24) {
        st.pos += 1;
        return BigInt(low);
    }
    if (low === 24) {
        const v = BigInt(st.b[st.pos + 1]);
        st.pos += 2;
        return v;
    }
    if (low === 25) {
        let v = 0n;
        for (let i = 1; i <= 2; i++)
            v = (v << 8n) | BigInt(st.b[st.pos + i]);
        st.pos += 3;
        return v;
    }
    if (low === 26) {
        let v = 0n;
        for (let i = 1; i <= 4; i++)
            v = (v << 8n) | BigInt(st.b[st.pos + i]);
        st.pos += 5;
        return v;
    }
    if (low === 27) {
        let v = 0n;
        for (let i = 1; i <= 8; i++)
            v = (v << 8n) | BigInt(st.b[st.pos + i]);
        st.pos += 9;
        return v;
    }
    throw new Error("malformed CBOR argument");
}
function smallInt(arg) {
    return arg <= BigInt(Number.MAX_SAFE_INTEGER) ? Number(arg) : arg;
}
function readFloat(st, low) {
    const bits = readArg(st, low);
    if (low === 26) {
        const view = new DataView(new ArrayBuffer(4));
        view.setUint32(0, Number(bits), false);
        return view.getFloat32(0, false);
    }
    const view = new DataView(new ArrayBuffer(8));
    view.setBigUint64(0, bits, false);
    return view.getFloat64(0, false);
}
function decInto(st) {
    const ib = st.b[st.pos];
    const major = ib >> 5;
    const low = ib & 0x1f;
    if (major === 7) {
        if (low === 20) {
            st.pos += 1;
            return false;
        }
        if (low === 21) {
            st.pos += 1;
            return true;
        }
        if (low === 22 || low === 23) {
            st.pos += 1;
            return null;
        }
        if (low === 26 || low === 27)
            return readFloat(st, low);
        throw new Error("malformed CBOR simple value");
    }
    const arg = readArg(st, low);
    switch (major) {
        case 0:
            return smallInt(arg);
        case 1: {
            const n = -1n - arg;
            return n >= BigInt(Number.MIN_SAFE_INTEGER) ? Number(n) : n;
        }
        case 2: {
            const n = Number(arg);
            const slice = st.b.slice(st.pos, st.pos + n);
            st.pos += n;
            return slice;
        }
        case 3: {
            const n = Number(arg);
            const text = csilTextDecoder.decode(st.b.subarray(st.pos, st.pos + n));
            st.pos += n;
            return text;
        }
        case 4: {
            const n = Number(arg);
            const arr = [];
            for (let i = 0; i < n; i++)
                arr.push(decInto(st));
            return arr;
        }
        case 5: {
            const n = Number(arg);
            const m = new Map();
            for (let i = 0; i < n; i++) {
                const k = decInto(st);
                const val = decInto(st);
                m.set(k, val);
            }
            return m;
        }
        case 6: {
            const inner = decInto(st);
            return { tag: Number(arg), value: inner };
        }
        default:
            throw new Error("malformed CBOR major type");
    }
}
/** Decode a CSIL CBOR byte payload into a CBOR value tree. */
function decode(bytes) {
    const st = { b: bytes, pos: 0 };
    const v = decInto(st);
    if (st.pos !== bytes.length)
        throw new Error("trailing bytes after CBOR value");
    return v;
}
/** The value for `key` in a CBOR map node, or `undefined` when absent. */
function mapGet(value, key) {
    return value instanceof Map ? value.get(key) : undefined;
}
/** The value for a required `key`; throws when the field is missing. */
function requireKey(value, key) {
    const v = mapGet(value, key);
    if (v === undefined)
        throw new Error(`missing required field: ${key}`);
    return v;
}
function asNumber(value) {
    if (typeof value === "number")
        return value;
    if (typeof value === "bigint")
        return Number(value);
    throw new Error("expected a number");
}
function asString(value) {
    if (typeof value === "string")
        return value;
    throw new Error("expected a text string");
}
function asBytes(value) {
    if (value instanceof Uint8Array)
        return value;
    throw new Error("expected a byte string");
}
function asBool(value) {
    if (typeof value === "boolean")
        return value;
    throw new Error("expected a boolean");
}
function asArray(value) {
    if (Array.isArray(value))
        return value;
    throw new Error("expected an array");
}
function asMap(value) {
    if (value instanceof Map)
        return value;
    throw new Error("expected a map");
}
function asTagged(value, tag) {
    if (typeof value === "object" &&
        value !== null &&
        !Array.isArray(value) &&
        !(value instanceof Uint8Array) &&
        !(value instanceof Map) &&
        value.tag === tag) {
        return value.value;
    }
    throw new Error(`expected tag ${tag}`);
}
/** Decode a tag-0 (RFC 3339, UTC) timestamp into a `Date`. */
function asTimestamp(value) {
    return new Date(asString(asTagged(value, 0)));
}
/** Decode a tag-4 decimal fraction into its `[exponent, mantissa]` payload. */
function asDecimalPayload(value) {
    const arr = asArray(asTagged(value, 4));
    const intOf = (x) => {
        if (typeof x === "number" || typeof x === "bigint")
            return x;
        throw new Error("expected an integer in decimal payload");
    };
    return [intOf(arr[0]), intOf(arr[1])];
}
/** A `Date` rendered as the canonical tag-0 text: RFC 3339, UTC, `Z` offset. */
function csilTsToText(d) {
    return d.toISOString().replace(/\.000Z$/, "Z");
}
function toTaskCborValue(v) {
    const csilMap = new Map();
    csilMap.set("uuid", v.uuid);
    csilMap.set("queue", v.queue);
    csilMap.set("payload", v.payload);
    csilMap.set("timeout", v.timeout);
    csilMap.set("priority", v.priority);
    csilMap.set("submit_time", v.submitTime);
    csilMap.set("update_time", v.updateTime);
    csilMap.set("current_state", v.currentState);
    csilMap.set("auto_target_state", v.autoTargetState);
    return csilMap;
}
function fromTaskCborValue(value) {
    return {
        uuid: asString(requireKey(value, "uuid")),
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
        autoTargetState: asString(requireKey(value, "auto_target_state")),
        submitTime: asNumber(requireKey(value, "submit_time")),
        updateTime: asNumber(requireKey(value, "update_time")),
        timeout: asNumber(requireKey(value, "timeout")),
        payload: asBytes(requireKey(value, "payload")),
        priority: asNumber(requireKey(value, "priority")),
    };
}
function toTaskCbor(v) {
    return encodeValue(toTaskCborValue(v));
}
function fromTaskCbor(bytes) {
    return fromTaskCborValue(decode(bytes));
}
function toSubmitTaskRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue", v.queue);
    csilMap.set("payload", v.payload);
    csilMap.set("timeout", v.timeout);
    csilMap.set("priority", v.priority);
    csilMap.set("current_state", v.currentState);
    csilMap.set("auto_target_state", v.autoTargetState);
    return csilMap;
}
function fromSubmitTaskRequestCborValue(value) {
    return {
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
        autoTargetState: asString(requireKey(value, "auto_target_state")),
        timeout: asNumber(requireKey(value, "timeout")),
        payload: asBytes(requireKey(value, "payload")),
        priority: asNumber(requireKey(value, "priority")),
    };
}
function toSubmitTaskRequestCbor(v) {
    return encodeValue(toSubmitTaskRequestCborValue(v));
}
function fromSubmitTaskRequestCbor(bytes) {
    return fromSubmitTaskRequestCborValue(decode(bytes));
}
function toSubmitTaskResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromSubmitTaskResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toSubmitTaskResponseCbor(v) {
    return encodeValue(toSubmitTaskResponseCborValue(v));
}
function fromSubmitTaskResponseCbor(bytes) {
    return fromSubmitTaskResponseCborValue(decode(bytes));
}
function toGetTaskStateByIDRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("uuid", v.uuid);
    csilMap.set("queue", v.queue);
    return csilMap;
}
function fromGetTaskStateByIDRequestCborValue(value) {
    return {
        uuid: asString(requireKey(value, "uuid")),
        queue: asString(requireKey(value, "queue")),
    };
}
function toGetTaskStateByIDRequestCbor(v) {
    return encodeValue(toGetTaskStateByIDRequestCborValue(v));
}
function fromGetTaskStateByIDRequestCbor(bytes) {
    return fromGetTaskStateByIDRequestCborValue(decode(bytes));
}
function toGetTaskStateByIDResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromGetTaskStateByIDResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toGetTaskStateByIDResponseCbor(v) {
    return encodeValue(toGetTaskStateByIDResponseCborValue(v));
}
function fromGetTaskStateByIDResponseCbor(bytes) {
    return fromGetTaskStateByIDResponseCborValue(decode(bytes));
}
function toGetNextTaskRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue", v.queue);
    csilMap.set("current_state", v.currentState);
    csilMap.set("override_timeout", v.overrideTimeout);
    csilMap.set("override_current_state", v.overrideCurrentState);
    csilMap.set("override_auto_target_state", v.overrideAutoTargetState);
    return csilMap;
}
function fromGetNextTaskRequestCborValue(value) {
    return {
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
        overrideTimeout: asNumber(requireKey(value, "override_timeout")),
        overrideCurrentState: asString(requireKey(value, "override_current_state")),
        overrideAutoTargetState: asString(requireKey(value, "override_auto_target_state")),
    };
}
function toGetNextTaskRequestCbor(v) {
    return encodeValue(toGetNextTaskRequestCborValue(v));
}
function fromGetNextTaskRequestCbor(bytes) {
    return fromGetNextTaskRequestCborValue(decode(bytes));
}
function toGetNextTaskResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromGetNextTaskResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toGetNextTaskResponseCbor(v) {
    return encodeValue(toGetNextTaskResponseCborValue(v));
}
function fromGetNextTaskResponseCbor(bytes) {
    return fromGetNextTaskResponseCborValue(decode(bytes));
}
function toCompleteTaskRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("uuid", v.uuid);
    csilMap.set("queue", v.queue);
    csilMap.set("current_state", v.currentState);
    return csilMap;
}
function fromCompleteTaskRequestCborValue(value) {
    return {
        uuid: asString(requireKey(value, "uuid")),
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
    };
}
function toCompleteTaskRequestCbor(v) {
    return encodeValue(toCompleteTaskRequestCborValue(v));
}
function fromCompleteTaskRequestCbor(bytes) {
    return fromCompleteTaskRequestCborValue(decode(bytes));
}
function toCompleteTaskResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromCompleteTaskResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toCompleteTaskResponseCbor(v) {
    return encodeValue(toCompleteTaskResponseCborValue(v));
}
function fromCompleteTaskResponseCbor(bytes) {
    return fromCompleteTaskResponseCborValue(decode(bytes));
}
function toUpdateTaskRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("uuid", v.uuid);
    csilMap.set("queue", v.queue);
    csilMap.set("payload", v.payload);
    csilMap.set("timeout", v.timeout);
    csilMap.set("priority", v.priority);
    csilMap.set("new_state", v.newState);
    csilMap.set("current_state", v.currentState);
    csilMap.set("auto_target_state", v.autoTargetState);
    return csilMap;
}
function fromUpdateTaskRequestCborValue(value) {
    return {
        uuid: asString(requireKey(value, "uuid")),
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
        autoTargetState: asString(requireKey(value, "auto_target_state")),
        timeout: asNumber(requireKey(value, "timeout")),
        newState: asString(requireKey(value, "new_state")),
        payload: asBytes(requireKey(value, "payload")),
        priority: asNumber(requireKey(value, "priority")),
    };
}
function toUpdateTaskRequestCbor(v) {
    return encodeValue(toUpdateTaskRequestCborValue(v));
}
function fromUpdateTaskRequestCbor(bytes) {
    return fromUpdateTaskRequestCborValue(decode(bytes));
}
function toUpdateTaskResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromUpdateTaskResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toUpdateTaskResponseCbor(v) {
    return encodeValue(toUpdateTaskResponseCborValue(v));
}
function fromUpdateTaskResponseCbor(bytes) {
    return fromUpdateTaskResponseCborValue(decode(bytes));
}
function toCancelTaskRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("uuid", v.uuid);
    csilMap.set("queue", v.queue);
    csilMap.set("current_state", v.currentState);
    return csilMap;
}
function fromCancelTaskRequestCborValue(value) {
    return {
        uuid: asString(requireKey(value, "uuid")),
        queue: asString(requireKey(value, "queue")),
        currentState: asString(requireKey(value, "current_state")),
    };
}
function toCancelTaskRequestCbor(v) {
    return encodeValue(toCancelTaskRequestCborValue(v));
}
function fromCancelTaskRequestCbor(bytes) {
    return fromCancelTaskRequestCborValue(decode(bytes));
}
function toCancelTaskResponseCborValue(v) {
    const csilMap = new Map();
    if (v.task !== undefined)
        csilMap.set("task", toTaskCborValue(v.task));
    return csilMap;
}
function fromCancelTaskResponseCborValue(value) {
    return {
        task: ((csilV) => csilV === undefined ? undefined : fromTaskCborValue(csilV))(mapGet(value, "task")),
    };
}
function toCancelTaskResponseCbor(v) {
    return encodeValue(toCancelTaskResponseCborValue(v));
}
function fromCancelTaskResponseCbor(bytes) {
    return fromCancelTaskResponseCborValue(decode(bytes));
}
function toCleanUpTimedOutRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue", v.queue);
    csilMap.set("at_time", v.atTime);
    return csilMap;
}
function fromCleanUpTimedOutRequestCborValue(value) {
    return {
        atTime: asNumber(requireKey(value, "at_time")),
        queue: asString(requireKey(value, "queue")),
    };
}
function toCleanUpTimedOutRequestCbor(v) {
    return encodeValue(toCleanUpTimedOutRequestCborValue(v));
}
function fromCleanUpTimedOutRequestCbor(bytes) {
    return fromCleanUpTimedOutRequestCborValue(decode(bytes));
}
function toCleanUpTimedOutResponseCborValue(v) {
    const csilMap = new Map();
    csilMap.set("timed_out", v.timedOut);
    return csilMap;
}
function fromCleanUpTimedOutResponseCborValue(value) {
    return {
        timedOut: asNumber(requireKey(value, "timed_out")),
    };
}
function toCleanUpTimedOutResponseCbor(v) {
    return encodeValue(toCleanUpTimedOutResponseCborValue(v));
}
function fromCleanUpTimedOutResponseCbor(bytes) {
    return fromCleanUpTimedOutResponseCborValue(decode(bytes));
}
function toGetQueuesRequestCborValue(v) {
    const csilMap = new Map();
    return csilMap;
}
function fromGetQueuesRequestCborValue(value) {
    void value;
    return {};
}
function toGetQueuesRequestCbor(v) {
    return encodeValue(toGetQueuesRequestCborValue(v));
}
function fromGetQueuesRequestCbor(bytes) {
    return fromGetQueuesRequestCborValue(decode(bytes));
}
function toGetQueuesResponseCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queues", v.queues);
    csilMap.set("total_task_count", v.totalTaskCount);
    return csilMap;
}
function fromGetQueuesResponseCborValue(value) {
    return {
        queues: asArray(requireKey(value, "queues")).map((csilE) => asString(csilE)),
        totalTaskCount: asNumber(requireKey(value, "total_task_count")),
    };
}
function toGetQueuesResponseCbor(v) {
    return encodeValue(toGetQueuesResponseCborValue(v));
}
function fromGetQueuesResponseCbor(bytes) {
    return fromGetQueuesResponseCborValue(decode(bytes));
}
function toGetQueueTaskCountsRequestCborValue(v) {
    const csilMap = new Map();
    return csilMap;
}
function fromGetQueueTaskCountsRequestCborValue(value) {
    void value;
    return {};
}
function toGetQueueTaskCountsRequestCbor(v) {
    return encodeValue(toGetQueueTaskCountsRequestCborValue(v));
}
function fromGetQueueTaskCountsRequestCbor(bytes) {
    return fromGetQueueTaskCountsRequestCborValue(decode(bytes));
}
function toGetQueueTaskCountsResponseCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue_counts", new Map(Object.entries(v.queueCounts).map(([csilK, csilV]) => [csilK, csilV])));
    csilMap.set("total_task_count", v.totalTaskCount);
    return csilMap;
}
function fromGetQueueTaskCountsResponseCborValue(value) {
    return {
        queueCounts: Object.fromEntries(Array.from(asMap(requireKey(value, "queue_counts")), ([csilK, csilV]) => [asString(csilK), asNumber(csilV)])),
        totalTaskCount: asNumber(requireKey(value, "total_task_count")),
    };
}
function toGetQueueTaskCountsResponseCbor(v) {
    return encodeValue(toGetQueueTaskCountsResponseCborValue(v));
}
function fromGetQueueTaskCountsResponseCbor(bytes) {
    return fromGetQueueTaskCountsResponseCborValue(decode(bytes));
}
function toGetTaskStateCountsRequestCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue", v.queue);
    return csilMap;
}
function fromGetTaskStateCountsRequestCborValue(value) {
    return {
        queue: asString(requireKey(value, "queue")),
    };
}
function toGetTaskStateCountsRequestCbor(v) {
    return encodeValue(toGetTaskStateCountsRequestCborValue(v));
}
function fromGetTaskStateCountsRequestCbor(bytes) {
    return fromGetTaskStateCountsRequestCborValue(decode(bytes));
}
function toGetTaskStateCountsResponseCborValue(v) {
    const csilMap = new Map();
    csilMap.set("count", v.count);
    csilMap.set("queue", v.queue);
    csilMap.set("state_counts", new Map(Object.entries(v.stateCounts).map(([csilK, csilV]) => [csilK, csilV])));
    return csilMap;
}
function fromGetTaskStateCountsResponseCborValue(value) {
    return {
        queue: asString(requireKey(value, "queue")),
        count: asNumber(requireKey(value, "count")),
        stateCounts: Object.fromEntries(Array.from(asMap(requireKey(value, "state_counts")), ([csilK, csilV]) => [asString(csilK), asNumber(csilV)])),
    };
}
function toGetTaskStateCountsResponseCbor(v) {
    return encodeValue(toGetTaskStateCountsResponseCborValue(v));
}
function fromGetTaskStateCountsResponseCbor(bytes) {
    return fromGetTaskStateCountsResponseCborValue(decode(bytes));
}
function toQueueAndStateCountsCborValue(v) {
    const csilMap = new Map();
    csilMap.set("count", v.count);
    csilMap.set("queue", v.queue);
    csilMap.set("state_counts", new Map(Object.entries(v.stateCounts).map(([csilK, csilV]) => [csilK, csilV])));
    return csilMap;
}
function fromQueueAndStateCountsCborValue(value) {
    return {
        queue: asString(requireKey(value, "queue")),
        count: asNumber(requireKey(value, "count")),
        stateCounts: Object.fromEntries(Array.from(asMap(requireKey(value, "state_counts")), ([csilK, csilV]) => [asString(csilK), asNumber(csilV)])),
    };
}
function toQueueAndStateCountsCbor(v) {
    return encodeValue(toQueueAndStateCountsCborValue(v));
}
function fromQueueAndStateCountsCbor(bytes) {
    return fromQueueAndStateCountsCborValue(decode(bytes));
}
function toGetQueueAndStateCountsRequestCborValue(v) {
    const csilMap = new Map();
    return csilMap;
}
function fromGetQueueAndStateCountsRequestCborValue(value) {
    void value;
    return {};
}
function toGetQueueAndStateCountsRequestCbor(v) {
    return encodeValue(toGetQueueAndStateCountsRequestCborValue(v));
}
function fromGetQueueAndStateCountsRequestCbor(bytes) {
    return fromGetQueueAndStateCountsRequestCborValue(decode(bytes));
}
function toGetQueueAndStateCountsResponseCborValue(v) {
    const csilMap = new Map();
    csilMap.set("queue_and_state_counts", new Map(Object.entries(v.queueAndStateCounts).map(([csilK, csilV]) => [csilK, toQueueAndStateCountsCborValue(csilV)])));
    return csilMap;
}
function fromGetQueueAndStateCountsResponseCborValue(value) {
    return {
        queueAndStateCounts: Object.fromEntries(Array.from(asMap(requireKey(value, "queue_and_state_counts")), ([csilK, csilV]) => [asString(csilK), fromQueueAndStateCountsCborValue(csilV)])),
    };
}
function toGetQueueAndStateCountsResponseCbor(v) {
    return encodeValue(toGetQueueAndStateCountsResponseCborValue(v));
}
function fromGetQueueAndStateCountsResponseCbor(bytes) {
    return fromGetQueueAndStateCountsResponseCborValue(decode(bytes));
}
function toServiceErrorCborValue(v) {
    const csilMap = new Map();
    csilMap.set("code", v.code);
    csilMap.set("message", v.message);
    return csilMap;
}
function fromServiceErrorCborValue(value) {
    return {
        code: asNumber(requireKey(value, "code")),
        message: asString(requireKey(value, "message")),
    };
}
function toServiceErrorCbor(v) {
    return encodeValue(toServiceErrorCborValue(v));
}
function fromServiceErrorCbor(bytes) {
    return fromServiceErrorCborValue(decode(bytes));
}
