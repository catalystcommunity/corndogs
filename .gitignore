.idea
.vscode

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# corndogs/vendor/

# Protobuf stuff

# Helm dependency stuff
helm_chart/chart/Chart.lock
helm_chart/chart/charts/

# Node nonsense
node_modules
package-lock.json

# Rust build artifacts (clients/rust). Cargo.lock is not committed for the
# generated client library.
target/
Cargo.lock

# Python build artifacts (clients/python)
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Go build output (the server binary is built into images/tmp, never committed)
corndogs/main

# Python venvs (client dev / verification)
.venv/
venv/

# Client build artifacts (per-language toolchains; never commit build output)
# C / native objects
*.o
*.a
# JVM (java, kotlin)
*.class
*.jar
clients/*/build/
.gradle/
# .NET (csharp)
clients/csharp/bin/
clients/csharp/obj/
# Elixir / Erlang
_build/
deps/
*.beam
erl_crash.dump
# OCaml
_opam/
*.cmo
*.cmi
*.cmx
# Zig
.zig-cache/
zig-cache/
zig-out/
# Dart
.dart_tool/
pubspec.lock
# Swift
.build/
